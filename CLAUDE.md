# Workspace rules

- Lessons are self-contained. There are no separate `show-me-*.html` files — when a
  lesson needs visuals, use the `show-me` skill and put its diagrams inline in the
  lesson under a `<h2 id="see-it-drawn">See it drawn</h2>` section, placed just
  before the closing `ask` footer.
